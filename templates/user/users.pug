extends ../layout

block tity
    title #{title}


block content
    h1 #{title}
    ul.list-group
        each user, i in users
            li.list-group-item 
                a(href="/users/"+user._id)= user.username
